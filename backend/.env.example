# Backend-only env file. When running via docker-compose the root .env is used
# instead; this file is for running the backend directly (uvicorn app.main:app).

DATABASE_URL=postgresql+psycopg://tiffin:tiffin@localhost:5432/gharse_tiffin
JWT_SECRET=change-me-in-production-please
JWT_EXPIRE_MINUTES=720
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
APP_TIMEZONE=Asia/Kolkata

# Comma-separated list of allowed CORS origins for the frontend.
CORS_ORIGINS=http://localhost:3000
