# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Database ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Auth ---
passlib[bcrypt]==1.7.4
# Pinned: passlib 1.7.4 reads bcrypt.__about__ which was removed in bcrypt >= 4.1
bcrypt==4.0.1
PyJWT==2.10.1

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# IANA timezone data (Windows / slim Linux images have no system tzdb)
tzdata==2024.2

# --- Tests ---
pytest==8.3.4
httpx==0.28.1
